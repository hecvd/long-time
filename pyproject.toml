[project]
name = "long-time"
version = "0.1.0"
description = "A minimal self-hosted time tracker"
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
