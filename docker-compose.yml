services:
  long-time:
    image: ghcr.io/hecvd/long-time:latest
    container_name: long-time
    environment:
      - PUID=1000
      - PGID=1000
    ports:
      - "5225:5225"
    volumes:
      - ./config:/config
    restart: unless-stopped
